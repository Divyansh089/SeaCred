{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2020",                       // Compile to a modern version of JavaScript
    "module": "CommonJS",                      // Use the standard Node.js module system
    "outDir": "./dist",                        // Where to output the compiled JavaScript
    "rootDir": "./src",                        // The root directory of your source files

    /* Module Resolution Options */
    "moduleResolution": "node",                // How to find modules (standard for Node)
    "esModuleInterop": true,                   // *** This is the key fix for the Express error ***

    /* Strict Type-Checking Options */
    "strict": true,                            // Enable all strict type-checking options
    "skipLibCheck": true,                      // Skip type checking of declaration files from libraries

    /* Advanced Options */
    "forceConsistentCasingInFileNames": true   // Disallow inconsistently-cased references to the same file
  },
  "include": [
    "src/**/*"                                 // Specifies which files TypeScript should compile
  ],
  "exclude": [
    "node_modules"                             // Specifies folders to exclude from compilation
  ]
}